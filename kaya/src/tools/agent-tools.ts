// ─── Agent Tool Modules ─────────────────────────────────────────────────────
// Declares which tool modules Kaya loads. Used by the registry at startup.

export const AGENT_TOOL_MODULES = ['core', 'leads', 'outreach', 'website-pitch'];
